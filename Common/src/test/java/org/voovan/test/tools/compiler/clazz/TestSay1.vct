package org.voovan.test.tools.compiler.clazz;

import org.voovan.tools.TEnv;
import org.voovan.tools.TFile;
import org.voovan.tools.TString;

public class TestSay1 {
    public String say() {
        try {
            System.out.println("helloword 123");
            System.out.println(TFile.fileExists("/Users/helyho/.zshrc"));
            TEnv.sleep(500);
            System.out.println(this.getClass().getClassLoader());
            return TString.removePrefix("ffinished");
        } finally {
            //System.exit(1);
        }
    }
}